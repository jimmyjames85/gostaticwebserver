{
    "basedir": "/www",
    "routes": {
	    "": "{{.basedir}}",
	    "base": "{{.basedir}}/index.html",
	    "dir1": "{{.basedir}}/dir1",
	    "dir2": "{{.basedir}}/dir2",
	    "dir2.subdir": "{{.basedir}}/dir2/subdir"
    }
}
